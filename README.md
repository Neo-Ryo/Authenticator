# Authenticator test server

### Introduction
This is a simple OTP server.

### Getting started
- `yarn install`
- `yarn start`

Server will start on given port and run with nodemon to allow auto-reload


#### Exposed Port:
- 9000

